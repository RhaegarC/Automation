!define WEB (!-SuiteManagedMetadata-!)
!define APPNAME (!-SuiteMetadata-!)
!define MIGRATION (!-DocLib2DocLibWordDoc-!)
!define TAGGINGFIELD (${LIST})
!define VIEW (!-SuiteMetadata-!)

!contents -R2 -g -p -f -h